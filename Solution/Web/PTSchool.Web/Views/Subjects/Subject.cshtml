@model PTSchool.Web.Models.Subject.SubjectLightViewModel

<div>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            <div class="text-center border border-dark p-2">
                <p>@Model.Id</p>
                <p>@Model.Name</p>
                <a href="@Url.Action("Subject", "Subjects", new { id = Model.Id })"><img src="@Model.Image" class="img-circle-sm" /></a>
            </div>
        </div>
        <div class="col-md-4"></div>
    </div>
</div>